{
  "name": "ha-state-timeline-card",
  "version": "0.1.0",
  "description": "Step through HA recorder state transitions across multiple entities.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "lit": "^3.1.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
